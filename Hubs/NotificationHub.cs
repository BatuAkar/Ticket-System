using Microsoft.AspNetCore.SignalR;

namespace TicketSistemi.Hubs
{
    public class NotificationHub : Hub
    {
        // Hub is used as an endpoint to broadcast notifications from controller to clients
    }
}
